plugins {
    id "com.android.application"
    id 'com.google.gms.google-services' // Firebase Configuration
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.commerce_store"
    compileSdk = 34 // Replace with your desired compileSdkVersion
    ndkVersion = "25.1.8937393" // Optional: Specify your NDK version

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.commerce_store"
        minSdk = 23
        targetSdk = 33
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // TODO: Configure release signing
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
